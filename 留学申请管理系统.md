<!-- 改编自：教育行业主题项目提示词/gb-12-1/留学申请管理系统.md；后端由 Django 4.x + Python 3.11 + Django REST Framework 改造为 Go 1.22 + Gin + GORM -->
# gb-12-1 留学申请管理系统

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**留学申请管理系统（gbstudyapply）** 面向留学中介机构和自助留学学生，帮助用户管理目标院校信息、准备申请材料、跟踪申请进度，覆盖从选校规划、文书撰写到录取结果跟踪的全流程。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **User（用户）**
  - 字段：id, username, email, password_hash, real_name, phone, role（student/counselor/admin）, created_at
  - 关键关系：可拥有多个 ApplicationProject、Document、Annotation、Recommendation、Message
  - 业务动作：注册/登录、修改资料、顾问与学生绑定
  - **此实体必须贯穿：数据库表 → backend/internal/model/user.go → backend/internal/repository/user_repository.go → backend/internal/service/user_service.go → backend/internal/handler/user_handler.go → backend/internal/router/users.go → frontend/src/api/user.ts → frontend/src/stores/userStore.ts → frontend/src/pages/Profile.tsx**

- **University（院校）**
  - 字段：id, name, country, city, ranking, top_majors（JSON）, application_deadline, tuition_range, requirements（JSON：GPA/语言/标化考试）, created_at
  - 关键关系：被多个 ApplicationProject 和 Recommendation 引用
  - 业务动作：院校库维护、按国家/排名/专业筛选、申请要求展示
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **ApplicationProject（申请项目）**
  - 字段：id, student_id, counselor_id, university_id, major, round, status（planning/preparing/submitted/waiting/admitted/rejected/waitlisted）, created_at, updated_at
  - 关键关系：关联 User（学生/顾问）与 University，拥有多个 Document、MaterialItem、TimelineNode
  - 业务动作：创建申请项目、状态流转、材料进度与时间线跟踪
  - **此实体必须贯穿全栈**

- **Document（文书）**
  - 字段：id, application_id, doc_type（PS/RL/CV/ESSAY）, title, content, current_version, created_at, updated_at
  - 关键关系：隶属于 ApplicationProject，拥有多个 DocumentVersion 和 Annotation
  - 业务动作：在线编辑、版本保存、顾问批注
  - **此实体必须贯穿全栈**

- **DocumentVersion（文书版本）**
  - 字段：id, document_id, content, version_no, change_summary, created_by, created_at
  - 关键关系：隶属于 Document
  - 业务动作：保存历史版本、版本对比（diff）、版本回滚
  - **此实体必须贯穿全栈**

- **Annotation（文书批注）**
  - 字段：id, document_id, counselor_id, content, start_offset, end_offset, created_at
  - 关键关系：隶属于 Document 与 User（顾问）
  - 业务动作：顾问添加/回复批注、学生查看反馈
  - **此实体必须贯穿全栈**

- **MaterialItem（材料清单项）**
  - 字段：id, application_id, name, category, is_required, status（pending/uploaded/approved）, file_url, uploaded_at, created_at
  - 关键关系：隶属于 ApplicationProject
  - 业务动作：生成标准化清单、逐项上传、标记完成、计算整体进度
  - **此实体必须贯穿全栈**

- **TimelineNode（时间线节点）**
  - 字段：id, application_id, title, node_type（language_exam/essay_deadline/application_deadline/interview/result）, due_date, is_done, reminder_sent, created_at
  - 关键关系：隶属于 ApplicationProject
  - 业务动作：生成关键节点、临近截止提醒、标记完成
  - **此实体必须贯穿全栈**

- **Recommendation（选校方案）**
  - 字段：id, student_id, counselor_id, university_ids（JSON）, reason, created_at
  - 关键关系：关联 User（学生/顾问）与多个 University
  - 业务动作：顾问推荐选校方案、学生查看推荐理由
  - **此实体必须贯穿全栈**

- **Message（站内消息）**
  - 字段：id, sender_id, receiver_id, content, is_read, created_at
  - 关键关系：关联发送者与接收者 User
  - 业务动作：学生与顾问沟通、系统发送截止提醒
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/dashboard（申请数据看板）**
  - 消费实体：ApplicationProject、User、MaterialItem
  - 关键交互：已申请院校数、各状态分布饼图、录取率、目标与实际对比、机构汇总数据
  - 共享组件：`<ApplicationStatusTag>`、`<StatCard>`

- **/universities（院校库）**
  - 消费实体：University
  - 关键交互：按国家、排名区间、专业方向筛选、院校列表
  - 共享组件：`<UniversityCard>`、`<SearchFilter>`

- **/universities/:id（院校详情）**
  - 消费实体：University
  - 关键交互：申请要求（GPA、语言、标化考试）、截止日期、学费范围展示
  - 共享组件：`<UniversityCard>`

- **/applications（申请项目列表）**
  - 消费实体：ApplicationProject、University、MaterialItem
  - 关键交互：项目列表、状态筛选、创建申请项目
  - 共享组件：`<ApplicationStatusTag>`（与 /dashboard 共用）、`<UniversityCard>`

- **/applications/:id（项目详情）**
  - 消费实体：ApplicationProject、TimelineNode、MaterialItem、Document
  - 关键交互：时间线展示、材料清单进度、文书入口
  - 共享组件：`<Timeline>`、`<MaterialProgress>`

- **/documents/:id（文书编辑）**
  - 消费实体：Document、DocumentVersion、Annotation
  - 关键交互：在线编辑、版本历史、diff 对比、顾问批注
  - 共享组件：`<DocumentDiff>`、`<AnnotationList>`

- **/timeline（时间线管理）**
  - 消费实体：TimelineNode、ApplicationProject
  - 关键交互：查看全部关键节点、截止提醒、标记完成
  - 共享组件：`<Timeline>`（与 /applications/:id 共用）

- **/messages（站内消息）**
  - 消费实体：Message
  - 关键交互：学生顾问沟通、系统提醒、已读/未读
  - 共享组件：`<MessageList>`

- **/counselor（顾问工作台）**
  - 消费实体：User、ApplicationProject、Recommendation、Annotation
  - 关键交互：查看绑定学生、文书批注、推荐选校方案
  - 共享组件：`<ApplicationStatusTag>`、`<AnnotationList>`（与 /documents/:id 共用）

- **/profile（个人中心）**
  - 消费实体：User
  - 关键交互：修改资料、学生/顾问绑定关系、账号信息
  - 共享组件：`<AvatarUploader>`

### 4. 横切关注点（≥ 2 个）

- **认证授权（JWT + RBAC）**
  - 触达文件层：
    - 数据库：`users` 表含 role 字段
    - 后端：`internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/util/jwt.go`
    - 路由：`backend/internal/router/*.go` 中学生专属接口需 `AuthRequired()` 和 `RequireRole(student)`，顾问/管理员接口需 `RequireRole(counselor/admin)`
    - 前端：`router/guards.ts`、`components/common/RoleGuard.tsx`、`stores/authStore.ts`

- **全局错误处理**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go`
    - 前端：`utils/request.ts` 拦截器

- **接口限流**
  - 触达文件层：
    - 后端：`internal/middleware/rate_limiter.go`
    - 路由：登录、文件上传等接口启用限流
    - 配置：`internal/config/config.go`

- **文件上传（MinIO 对象存储）**
  - 触达文件层：
    - 后端：`internal/handler/upload_handler.go`、`internal/service/upload_service.go`、`internal/util/minio_client.go`
    - 路由：文书附件、申请材料上传接口
    - 前端：`components/common/UploadButton.tsx`、`utils/request.ts`

- **截止日期提醒通知**
  - 触达文件层：
    - 后端：`internal/service/notification_service.go`、`internal/handler/notification_handler.go`、`internal/util/scheduler.go`
    - 路由：站内消息接口
    - 前端：`hooks/useDeadlineReminders.ts`、`components/common/ReminderToast.tsx`

### 5. 共享枚举/常量（≥ 2 组）

- **ApplicationStatus（申请状态）**
  - 值：PLANNING = 'planning', PREPARING = 'preparing', SUBMITTED = 'submitted', WAITING = 'waiting', ADMITTED = 'admitted', REJECTED = 'rejected', WAITLISTED = 'waitlisted'
  - 前端定义位置：`frontend/src/constants/application.ts`
  - 后端定义位置：`backend/internal/constants/application.go`
  - 出现位置：ApplicationProject 模型、ApplicationService、ApplicationStatusTag、项目列表、看板图表、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **DocumentType（文书类型）**
  - 值：PS = 'ps'（个人陈述）, RL = 'rl'（推荐信）, CV = 'cv'（简历）, ESSAY = 'essay'（Essay）
  - 前端定义位置：`frontend/src/constants/document.ts`
  - 后端定义位置：`backend/internal/constants/document.go`
  - 出现位置：Document 模型、DocumentService、文书编辑器、文档列表、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **MaterialStatus（材料状态）**
  - 值：PENDING = 'pending', UPLOADED = 'uploaded', APPROVED = 'approved'
  - 前端定义位置：`frontend/src/constants/material.ts`
  - 后端定义位置：`backend/internal/constants/material.go`
  - 出现位置：MaterialItem 模型、MaterialService、MaterialProgress、材料清单、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **UserRole（用户角色）**
  - 值：STUDENT = 'student', COUNSELOR = 'counselor', ADMIN = 'admin'
  - 前端定义位置：`frontend/src/constants/user.ts`
  - 后端定义位置：`backend/internal/constants/user.go`
  - 出现位置：User 模型、RBAC 中间件、路由权限、前端按钮显隐、路由守卫
  - **README 必须列出该枚举在前后端的所有出现位置**

### 6. 共享前端组件清单（≥ 3 个）

- `<ApplicationStatusTag>`：被 /dashboard、/applications、/counselor 共用
- `<Timeline>`：被 /applications/:id 和 /timeline 共用
- `<UniversityCard>`：被 /universities、/universities/:id、/applications 共用
- `<AnnotationList>`：被 /documents/:id 和 /counselor 共用
- `<MaterialProgress>`：被 /applications/:id 使用
- `<DocumentDiff>`：被 /documents/:id 使用
- `<StatCard>`：被 /dashboard 使用
- `<UploadButton>`：被 /documents/:id 和 /applications/:id 使用
- `<AvatarUploader>`：被 /profile 使用

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useAuth()`：登录态与角色判断
- `useApplicationStats()`：申请状态分布与录取率统计
- `useDocumentVersions()`：文书版本历史与 diff 加载
- `useDeadlineReminders()`：临近截止日期提醒
- `utils/dateFormat.ts`：日期时间格式化
- `utils/diff.ts`：文书版本 diff 对比（基于 diff-match-patch）

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将用户信息注入 gin.Context（`c.Set("user", ...)`）
- `rbac.go`：基于用户角色校验权限
- `rate_limiter.go`：接口限流
- `error_handler.go`：统一错误响应格式
- `logger.go`：请求日志记录
- `cors.go`：跨域配置

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`gbstudyapply`
- **前端端口**：8012
- **后端端口**：3012
- **前端框架**：React + TypeScript
- **UI 库**：Ant Design + ECharts
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：PostgreSQL + MinIO（文书/申请材料存储）
- **ORM**：GORM
- **认证方式**：JWT + RBAC（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/postgres`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`、`github.com/go-playground/validator/v10`、`github.com/minio/minio-go/v7`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # user.ts, university.ts, application.ts, document.ts, material.ts, timeline.ts, recommendation.ts, message.ts
├── stores/           # authStore.ts, userStore.ts, applicationStore.ts, documentStore.ts, universityStore.ts
├── types/            # 共享类型定义
├── components/common/# ApplicationStatusTag, Timeline, UniversityCard, AnnotationList, MaterialProgress, DocumentDiff, StatCard, UploadButton, AvatarUploader
├── hooks/            # useAuth.ts, useApplicationStats.ts, useDocumentVersions.ts, useDeadlineReminders.ts
├── pages/            # Dashboard, UniversityList, UniversityDetail, ApplicationList, ApplicationDetail, DocumentEditor, Timeline, Messages, CounselorWorkbench, Profile, Login
├── router/           # index.ts + guards.ts
├── utils/            # dateFormat.ts, diff.ts, request.ts
└── constants/        # application.ts, document.ts, material.ts, user.ts, errorCodes.ts
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB/MinIO、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go（数据库连接、MinIO、JWT、限流、端口等）
    ├── model/                   # 按实体分文件：user.go, university.go, application_project.go, document.go, document_version.go, annotation.go, material_item.go, timeline_node.go, recommendation.go, message.go
    ├── repository/              # 按实体分文件：user_repository.go, university_repository.go, application_project_repository.go, document_repository.go, document_version_repository.go, annotation_repository.go, material_item_repository.go, timeline_node_repository.go, recommendation_repository.go, message_repository.go
    ├── service/                 # 按实体分文件：user_service.go, university_service.go, application_project_service.go, document_service.go, document_version_service.go, annotation_service.go, material_item_service.go, timeline_node_service.go, recommendation_service.go, message_service.go, notification_service.go
    ├── handler/                 # 按实体分文件：user_handler.go, university_handler.go, application_project_handler.go, document_handler.go, document_version_handler.go, annotation_handler.go, material_item_handler.go, timeline_node_handler.go, recommendation_handler.go, message_handler.go, upload_handler.go
    ├── router/                  # router.go + 按实体分文件：users.go, universities.go, applications.go, documents.go, materials.go, timelines.go, recommendations.go, messages.go
    ├── middleware/              # auth.go, rbac.go, rate_limiter.go, error_handler.go, logger.go, cors.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # application.go, document.go, material.go, user.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, logger.go, formatters.go, app_error.go, minio_client.go, scheduler.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 25 条模板，如 `APPLICATION_STATUS_CHANGE_SUCCESS`、`DOCUMENT_SAVE_FAILED`
   - 任何业务字段变更（如 ApplicationProject 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名、字段名、角色名，如 `"ApplicationProject[id=123] status change failed: invalid transition"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含日期格式化、费用范围格式化、状态文本、角色文本、文书类型文本
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **申请状态机必须跨多处定义**
   - ApplicationStatus 的流转规则在 service 状态机、前端按钮显隐、日志模板、错误码、formatters 中同时存在
   - 新增一个状态值需要修改至少 10 处

5. **枚举必须前后端重复定义且被多处引用**
   - ApplicationStatus、DocumentType、MaterialStatus 和 UserRole 必须在前端 constants、后端 constants、类型定义、日志模板、错误码、formatters、GORM 模型中同时存在
   - 新增一个枚举值时，必须修改至少 10 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 ApplicationProject 新增一个 'deferred' 延期入学结果状态"，必须触达：GORM AutoMigrate、ApplicationProject 模型、前后端 constants、ApplicationService、ApplicationStatusTag、看板图表、formatters、日志模板、错误码、状态筛选器等 ≥ 10 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbstudyapply`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbstudyapply`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-gbstudyapply}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- MinIO 服务同样配置 `healthcheck`，后端服务等待其就绪
- 后端服务也配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `8012:80`，后端 `${BACKEND_PORT:-3012}:8080`，数据库 `${DB_PORT:-5505}:5432`，MinIO API `${MINIO_PORT:-9009}:9000`，MinIO 控制台 `${MINIO_CONSOLE_PORT:-9010}:9001`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbstudyapply`
- 数据库连接相关：`DB_NAME=gbstudyapply_db`、`DB_USER=gbstudyapply_user`、`DB_PASSWORD=gbstudyapply_pwd`、`DB_ROOT_PASSWORD=gbstudyapply_root`
- MinIO：`MINIO_ROOT_USER=minioadmin`、`MINIO_ROOT_PASSWORD=minioadmin`、`MINIO_PORT=9009`、`MINIO_CONSOLE_PORT=9010`
- 后端：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=8012`、`BACKEND_PORT=3012`、`DB_PORT=5505`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
backend/coverage/
frontend/coverage/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 ApplicationStatus、DocumentType、MaterialStatus 和 UserRole 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:8012` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<ApplicationStatusTag>` 或 `<Timeline>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
